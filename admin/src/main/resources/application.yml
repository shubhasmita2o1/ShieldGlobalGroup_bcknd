spring:
  application:
    name: admin
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
  jpa:
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: ${JWT_SECRET:shieldglobal_super_secret_key_2026_do_not_share_xYzAbC}
  expiration-ms: ${JWT_EXPIRATION_MS:28800000}

file:
  upload-dir: ${FILE_UPLOAD_DIR:uploads}

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000,https://admin.shieldglobalgroup.com}